# vim: ft=perl
use Morpheus;

$scalar = 1;
@array = (1, 2);
%hash = (1 => 2, 3 => 4);

$normalize = { "x/y" => 1 };

$override = { 
    "file/explicit" => 1,
    "file/implecit" => 1,
    "path" => 1,
};

$master = 1;
$slave = morph("test/file/stash/master") + 1;

$super = 1;

return { returned => 1 };
